name: Run Laravel Tests
description: Install Laravel dependencies and run Pest tests

inputs:
  php:
    description: PHP version to use
    required: false
    default: '8.4'
  laravel:
    description: Laravel version constraint (e.g. "11.*")
    required: false
    default: '12.*'
  working-directory:
    description: Working directory to run Composer commands in
    required: false
    default: '.'

runs:
  using: composite
  steps:
    - name: Setup Composer
      uses: lepusinc/.github/.github/actions/php/setup-composer@v1
      with:
        php: ${{ inputs.php }}
        working-directory: ${{ inputs.working-directory }}

    - name: Install Laravel dependencies
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: composer require --dev "laravel/framework:${{ inputs.laravel }}" --no-interaction --no-update

    - name: Run tests
      uses: lepusinc/.github/.github/actions/php/run-test@v1
      with:
        php: ${{ inputs.php }}
        working-directory: ${{ inputs.working-directory }}
